apply plugin: 'java'
apply plugin: 'idea'

dependencies {
    compile files('D:/Program Files (x86)/JetBrains/IntelliJ IDEA Community Edition 12.0/lib/junit-4.10.jar')
	runtime files('D:/Program Files (x86)/JetBrains/IntelliJ IDEA Community Edition 12.0/lib/junit-4.10.jar')
}

defaultTasks 'clean', 'build', 'test', 'jar', 'sourceJar', 'idea'
sourceSets {
    main {
        java {
            srcDir 'src/main'
        }
    }
    test {
        java {
            srcDir 'src/test'
        }
    }
}

tasks.withType(Compile) {
    options.encoding = 'UTF-8'
}

jar.baseName = 'parkingLotsModel'

task sourceJar(type: Jar) {
    from sourceSets.main.allSource
	from sourceSets.test.allSource
	baseName = 'parkingLotsModel'
}